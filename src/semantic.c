#include "ast.h"
#include "types.h"

int validate_ast(ast *tree);

int validate_ast(ast *tree) {
  /* todo
   *  - type checking
   *  - declared before used
   *  - move symbol table creation here?
   * */
  return 0;
}
